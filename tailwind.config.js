const colors = require('tailwindcss/colors');
/** @type {import('tailwindcss/types').Config} */
const config = {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts}"
  ],
  theme: {
    extend: {
    },
  },
  plugins: [],
}

module.exports = config